# Database
DB_NAME=koda
DB_USER=koda
DB_PASSWORD=changeme

# API security — must be at least 32 characters (256 bits)
JWT_SECRET=change-this-to-a-secure-random-string-at-least-32-characters-long

# Set to the public URL of the app (used for CORS)
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Optional
SERVER_PORT=8080
DB_PORT=5432
