{
  "name": "api",
  "version": "0.0.1",
  "description": "Koda API Service",
  "private": true,
  "scripts": {
    "dev": "./gradlew bootRun --args='--spring.profiles.active=local'",
    "build": "chmod +x ./gradlew && ./gradlew bootJar --no-daemon -x test",
    "start": "java -jar build/libs/api.jar",
    "test": "./gradlew test"
  }
}
