DB_HOST=localhost
DB_PORT=5432
DB_NAME=koda
DB_USER=koda
DB_PASSWORD=koda

SERVER_PORT=8080

# Must be at least 32 characters (256 bits) for HMAC-SHA256
JWT_SECRET=dev-secret-change-in-production-must-be-256-bits-long

# Comma-separated list of allowed CORS origins
CORS_ALLOWED_ORIGINS=http://localhost:4200
